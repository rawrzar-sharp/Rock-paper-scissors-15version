# TODO
- [x] Restore normal fonts by removing `Courier New` override (main font now inherits from `body`).
- [x] Start refactor: split UI into components under `frontend/src/components/`.
- [x] Create `RotatingCircle` component and its CSS.
- [x] Create `MainMenu` component and its CSS.
- [x] Create `RoundMenu` component and its CSS.
- [x] Create `SettingsModal` component and its CSS.
- [x] Create `RulesModal` component and its CSS.
- [x] Update `frontend/src/App.js` to become the state/view controller and render new components.
- [ ] Remove/reduce `frontend/src/styles.css` (currently still contains old styles).
- [ ] Verify build passes (TypeScript/React compile) and the UI still renders correctly.
- [ ] Finalize refactor: ensure `App.js` no longer contains old JSX/state remnants.
 

