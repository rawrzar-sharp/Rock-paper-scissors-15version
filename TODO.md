# TODO - CSS + hand images integration

- [x] Integrate App.js with styles.css (replace `./MainMenu.css` import).
- [x] Update `frontend/src/styles.css` background to dark blue so white text remains visible.


- [x] Update rotating hand circle in `frontend/src/App.js` to render `images/hand1.png` ... `images/hand15.png` instead of numeric placeholders.

- [x] Ensure images load correctly from the `images/` folder (adjust paths as needed).

- [x] Prevent images from drifting / keep centered by setting transform origin + disabling drag.

- [x] (Optional) Remove/adjust placeholder styling so images fit (size + border-radius).

- [x] Update settings modal background color so it doesn't conflict with font colors.


