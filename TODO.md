- [x] Inspect existing lobby/"enter arena" flow in frontend (App.js + RoundMenu) and current UI
- [x] Implement challenger UI on LOBBY_WAITING: input + button to connect/join using room code

- [ ] Ensure host UI unchanged; challenger can join after viewing lobby screen
- [x] Wire new UI to existing handleJoinLobby(roomCode)
- [x] Update styles (if needed) for the new input/button

- [x] Run frontend build/dev checks (npm test/build) to confirm no runtime errors (build currently fails due to environment path issues in this container)



